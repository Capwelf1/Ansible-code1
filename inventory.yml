[db]
172.31.45.106 
[ws]
172.31.25.18
[nv:children]
db
ws

# [webservers:vars]
# ansible_ssh_private_key_file=~/ansible-key.pem
# ansible_user= ec2-user

# [databaseservers:vars]
# ansible_ssh_private_key_file=~/ansible-key.pem
# ansible_user= ubuntu
